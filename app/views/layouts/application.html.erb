<!DOCTYPE html>
<html>
  <head>
    <title>DailyLog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="bg-gray-100 m-0">
    <header class="bg-white py-4 shadow">
      <div class="w-5/6 mx-auto">
        <div class="flex justify-between mx-4">
          <div>
            <h1 class="font-bold text-yellow-500">DailyLog</h1>
          </div>
          <div>
          <% if current_user %>
            <ul>
              <li class="inline-block hover:text-yellow-500 mr-4"><%= link_to "Dashboard", root_url %></li>
              <li class="inline-block hover:text-yellow-500"><%= link_to "Log Out", logout_path %></li>
            </ul>
          <% else %>
            <%= link_to "Sign Up", signup_path, class: "hover:text-yellow-500" %> or 
            <%= link_to "Log In", login_path, class: "hover:text-yellow-500" %>
          <% end %>
          </div>
        </div>
      </div>
    </header>
    <% flash.each do |key, value| %>
      <div class="bg-gray-200 text-gray-500">
        <div class="w-11/12 lg:w-5/6 mx-auto py-4">
            <p class="mx-4"><%= value %></p>
        </div>
      </div>
    <% end %>
    <div class="w-11/12 lg:w-5/6 mx-auto py-4">
      
      <%= yield %>
    </div>
  </body>
</html>
